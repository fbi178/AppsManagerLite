name: Build 啵啵-ManagerLite
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
      with:
        path: App

    - name: Setup
      run: |
        brew install ldid 2>/dev/null || true
        git clone --recursive --depth 1 https://github.com/theos/theos.git /tmp/theos

    - name: Build
      run: |
        export THEOS=/tmp/theos
        cd App
        make 2>&1

    - name: Pack TIPA
      run: |
        APP=$(find App/.theos -name "bobomanagerlite.app" -type d -not -path "*/arm64/*" | head -1)
        echo "App: $APP"
        echo -n "APPL????" > "$APP/PkgInfo"
        mkdir -p Payload
        cp -a "$APP" Payload/
        zip -X -r app.tipa Payload
        ls -la *.tipa

    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: 啵啵-ManagerLite
        path: app.tipa
